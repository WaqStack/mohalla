{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Preset for type-only shared packages that are consumed from source.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  }
}
