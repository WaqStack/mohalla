{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Preset for Node applications that emit JavaScript (api, worker).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "node16",
    "moduleResolution": "node16",
    "types": ["node"]
  }
}
