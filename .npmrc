# Fail rather than warn when the local Node/npm does not match the pin in
# package.json engines. The pinned toolchain is documented in
# docs/foundation/04-toolchain-versions.md.
engine-strict=true

# Reproducibility: never write a lockfile silently during CI installs.
save-exact=true
fund=false
audit=false
